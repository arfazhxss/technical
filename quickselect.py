class solution:
    def findKthLargest(self, nums: List[int], k: int) -> int:
        k = len(num) - k


def quickSelect(l, r):
    pivot, p = nums[r], lfor i in range(l, r):
    if (nums[i]) <= pivot:
